// import the data
// define the data as a variable
// write a function that separates the chunks newline separated chunks of data into a list/array (whatver they're called in C++)
// write a function that iterates through every chunk and adds them together, pushing those into a new array
// return the highest number